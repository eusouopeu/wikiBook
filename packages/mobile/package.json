{
  "name": "lexicon-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Base de conhecimento pessoal com grafo de conceitos — mobile (Capacitor)",
  "scripts": {
    "watch:web": "mkdir -p dist && cp index.html dist/index.html && esbuild src/main.tsx --bundle --outfile=dist/main.js --watch",
    "build:web": "mkdir -p dist && cp index.html dist/index.html && esbuild src/main.tsx --bundle --outfile=dist/main.js --minify",
    "sync": "npm run build:web && npx cap sync",
    "open:ios": "npm run sync && npx cap open ios",
    "open:android": "npm run sync && npx cap open android"
  },
  "dependencies": {
    "@lexicon/shared": "*",
    "@capacitor/core": "^8.4.2",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/preferences": "^8.0.1",
    "@capacitor/share": "^8.0.1",
    "capacitor-secure-storage-plugin": "^0.13.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2",
    "@capacitor/ios": "^8.4.2",
    "@capacitor/android": "^8.4.2",
    "@types/react": "^18.3.1",
    "@types/react-dom": "^18.3.0"
  }
}
